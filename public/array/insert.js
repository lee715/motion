// Generated by CoffeeScript 1.6.3
(function() {
  define(function() {
    var insert;
    return insert = function(arr, data, index) {
      var next, prev;
      index = index || 0;
      if (index) {
        prev = arr.slice(0, index);
        next = arr.slice(index);
        return arr = prev.concat(data, next);
      } else {
        return arr.unshift(data);
      }
    };
  });

}).call(this);
